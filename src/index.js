import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App /> {/* App rendered by index.js  to reactDOM*/}
  </React.StrictMode>,
  document.getElementById('root') 
);
